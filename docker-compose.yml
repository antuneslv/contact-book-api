services:
  postgres:
    image: postgres:16-alpine

    ports:
      - "5432:5432"

    environment:
      POSTGRES_USER: cb_user
      POSTGRES_PASSWORD: cb_pw
      POSTGRES_DB: contact_book_db

    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: